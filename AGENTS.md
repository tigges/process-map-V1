# AGENTS.md

## Cursor Cloud specific instructions

This repository (`process-map-V1`) is currently a **blank/greenfield project** with no application code, dependencies, or build system. As of setup, it contains only a `README.md`.

### Key facts
- **No package manager or lockfile** — no `package.json`, `requirements.txt`, `pyproject.toml`, `Gemfile`, `go.mod`, etc.
- **No build/test/lint commands** — there are no scripts, Makefiles, or CI configs.
- **No services to run** — no application, database, or external dependencies.

### What to do when code is added
Once application code is scaffolded, update this file and the VM update script with the appropriate dependency installation commands and startup instructions.
